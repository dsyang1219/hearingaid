openai>=1.30
sounddevice>=0.4.6
numpy>=1.24         # only used to resample TTS audio if the output device rejects 24kHz
webrtcvad>=2.0.10  # on Windows, `pip install webrtcvad-wheels` avoids needing a C compiler
pynput>=1.7        # only needed for --ptt (push-to-talk) mode
