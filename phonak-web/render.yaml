services:
  - type: web
    name: phonak-translator-web
    runtime: python
    plan: free
    rootDir: phonak-web
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false
